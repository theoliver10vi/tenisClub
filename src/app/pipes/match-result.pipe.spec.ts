import { MatchResultPipe } from './match-result.pipe';

describe('MatchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchResultPipe();
    expect(pipe).toBeTruthy();
  });
});
